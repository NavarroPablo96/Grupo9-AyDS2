package gestorInterfazOperador;

import java.awt.EventQueue;

import vista_controlador.ConexionFrame;

public class GestorFila {
	
    public static void main(String[] args) {
        // Lanzamos la aplicación en el hilo de la interfaz gráfica
        EventQueue.invokeLater(() -> {
            try {
                // Crear la ventana de conexión
                ConexionFrame conexionFrame = new ConexionFrame();
                conexionFrame.setVisible(true);
            } catch (Exception e) {
                e.printStackTrace();
            }
        });
    }

}
